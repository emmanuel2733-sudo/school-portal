<!DOCTYPE html>
<html>
<head>
  <title>Edit Topic</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
  <div class="card shadow-sm">
    <div class="card-body">
      <h3 class="mb-3">
        Edit Topic — <%= topic.course_name %> (<%= topic.class_name %>)
      </h3>

      <form method="POST" action="/teacher/topics/update/<%= topic.id %>">
        <div class="mb-3">
          <label class="form-label">Topic Name</label>
          <input type="text"
                 name="topic_name"
                 class="form-control"
                 value="<%= topic.topic_name %>"
                 required>
        </div>

        <button class="btn btn-primary">Save Changes</button>
        <a href="/teacher/topics/view/<%= topic.course_id %>" class="btn btn-secondary">Cancel</a>
      </form>
    </div>
  </div>
</div>

</body>
</html>
