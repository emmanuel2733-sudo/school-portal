<!DOCTYPE html>
<html>
<head>
    <title>Edit Question</title>
    <link rel="stylesheet" href="/style.css">

    <style>
        body {
            background:#f5f5f5;
            font-family: Arial;
            padding: 20px;
        }

        .container {
            max-width: 900px;
            margin: auto;
            background: white;
            padding: 25px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }

        h1 {
            margin-top: 0;
            text-align:center;
        }

        textarea {
            width: 100%;
            height: 130px;
            padding: 10px;
            font-size: 1em;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            font-size: 1em;
            margin-bottom: 15px;
        }

        select {
            width: 100%;
            padding: 10px;
            font-size: 1em;
        }

        .btn {
            padding: 10px 15px;
            border-radius:5px;
            text-decoration:none;
            border:none;
            cursor:pointer;
            font-size:1em;
        }

        .btn-primary {
            background:#007bff;
            color:white;
        }

        .btn-secondary {
            background:#6c757d;
            color:white;
        }

      .image-preview {
    width: 100%;
    max-width: 100%;
    overflow: hidden;
    text-align: center;
}

.image-preview img {
    width: 100%;
    max-width: 300px;   /* hard limit */
    height: auto;
    max-height: 250px;
    object-fit: contain;
    border-radius: 8px;
    border: 1px solid #ccc;
}

    </style>
</head>

<body>

<div class="container">

    <h1>Edit Question</h1>

    <form method="POST" enctype="multipart/form-data">

        <label><strong>Question:</strong></label>
        <textarea name="question" required><%= question.question %></textarea>

        <!-- SHOW EXISTING IMAGE -->
        <% if (question.image_path) { %>
            <div class="image-preview">
                <img src="/uploads/questions/<%= question.image_path %>" alt="Question Image">
            </div>
        <% } %>

        <!-- IMAGE UPLOAD -->
        <label><strong>Change Image:</strong></label><br>
        <input type="file" name="question_image">



        <br><br>

        <label><strong>Option A:</strong></label>
        <input type="text" name="option1" value="<%= options[0].option_text %>" required>

        <label><strong>Option B:</strong></label>
        <input type="text" name="option2" value="<%= options[1].option_text %>" required>

        <label><strong>Option C:</strong></label>
        <input type="text" name="option3" value="<%= options[2].option_text %>" required>

        <label><strong>Option D:</strong></label>
        <input type="text" name="option4" value="<%= options[3].option_text %>" required>

        <label><strong>Correct Answer:</strong></label>
        <select name="correct" required>
            <option value="1" <%= options[0].is_correct ? 'selected' : '' %>>A</option>
            <option value="2" <%= options[1].is_correct ? 'selected' : '' %>>B</option>
            <option value="3" <%= options[2].is_correct ? 'selected' : '' %>>C</option>
            <option value="4" <%= options[3].is_correct ? 'selected' : '' %>>D</option>
        </select>

        <br><br>

        <button class="btn btn-primary">Save Changes</button>
    </form>

    <br>

    <a href="/teacher/cbt/<%= examId %>/questions" class="btn btn-secondary">⬅ Back</a>

</div>

</body>
</html>
