<!DOCTYPE html>
<html>
<head>
    <title><%= course.name %> - Students</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .course-header { background: #007bff; color: white; padding: 15px; border-radius: 5px; margin-bottom: 20px; }
        table { border-collapse: collapse; width: 100%; margin: 20px 0; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        a { display: inline-block; padding: 10px; margin: 5px; background: #28a745; color: white; text-decoration: none; border-radius: 3px; }
        .grades-link { background: #ffc107 !important; color: black !important; }
        .back-link { background: #6c757d !important; }
        .logout { background: #dc3545 !important; }
        .no-students { text-align: center; padding: 40px; color: #666; }
    </style>
</head>
<body>
    
    <div class="course-header">
        <h1><%= course.name %> (Class: <%= course.class_name %>)</h1>
        <p>Manage students enrolled in this course</p>
    </div>
    
    <% if (students && students.length > 0) { %>
        <table>
            <tr>
                <th>Student</th>
                <th>Terms Enrolled</th>
                <th>Actions</th>
            </tr>
            <% students.forEach(student => { %>
                <tr>
                    <td><%= student.username %> - <%= student.name %></td>
                    <td><%= student.terms || 'N/A' %></td>
                    <!-- In the Actions column -->
<td>
    <a href="/teacher/course/<%= course.id %>/grades" class="grades-link">Manage Grades</a>
</td>
                </tr>
            <% }) %>
        </table>
    <% } else { %>
        <div class="no-students">
            <h3>No Students Enrolled</h3>
            <p>No students are currently enrolled in this course.</p>
        </div>
    <% } %>
    
    <div style="margin-top: 20px;">
        <a href="/teacher/courses" class="back-link">Back to My Courses</a>
        <a href="/dashboard">Dashboard</a>
        <a href="/logout" class="logout">Logout</a>
    </div>
</body>
</html>