<!DOCTYPE html>
<html>
<head>
  <title>School Portal - Create User</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 0; background: #f0f2f5; }
    .container { max-width: 480px; margin: 40px auto; padding: 30px; background: white; border-radius: 12px; box-shadow: 0 4px 20px rgba(0,0,0,0.1); }
    h1 { text-align: center; color: #2c3e50; margin-bottom: 20px; }
    .msg { padding: 12px; margin-bottom: 15px; border-radius: 8px; font-size: 14px; }
    .success { background: #d4edda; color: #155724; }
    .error { background: #f8d7da; color: #721c24; }
    .form-group { margin-bottom: 18px; }
    label { display: block; margin-bottom: 6px; color: #34495e; font-weight: 600; }
    input, select { width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 8px; box-sizing: border-box; font-size: 15px; }
    input:focus, select:focus { border-color: #007bff; outline: none; box-shadow: 0 0 0 3px rgba(0,123,255,0.1); }
    button { width: 100%; padding: 12px; background: #007bff; color: white; border: none; border-radius: 8px; cursor: pointer; font-size: 16px; font-weight: 600; margin-top: 10px; }
    button:hover { background: #0056b3; }
    .back-link { text-align: center; margin-top: 20px; }
    .back-link a { color: #007bff; text-decoration: none; }
    .photo-preview {
      width: 130px; height: 130px; border-radius: 50%; object-fit: cover;
      margin: 10px auto; display: block; border: 4px solid #fff; box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    }
    .upload-label { display: block; text-align: center; margin-top: 10px; color: #28a745; cursor: pointer; font-weight: 600; }
  </style>
</head>
<body>
  <div class="container">
    <h1>Create New User</h1>

    <% if (successMsg) { %>
      <div class="msg success"><%= successMsg %></div>
    <% } %>
    <% if (errorMsg) { %>
      <div class="msg error"><%= errorMsg %></div>
    <% } %>
    <% if (errors && errors.length > 0) { %>
      <div class="msg error">
        <% errors.forEach(error => { %>
          <p><%= error.msg %></p>
        <% }) %>
      </div>
    <% } %>

    <form action="/register" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label>Profile Photo</label>
        <img src="/uploads/default-photo.jpg" alt="Preview" class="photo-preview" id="preview">
        <input type="file" name="photo" accept="image/jpeg,image/png" onchange="previewPhoto(event)" style="display:none">
        <label class="upload-label" onclick="this.previousElementSibling.click()">Choose Photo (Optional)</label>
      </div>

      <div class="form-group">
        <label>Username</label>
        <input type="text" name="username" value="<%= userInput?.username || '' %>" required>
      </div>

      <div class="form-group">
        <label>Password</label>
        <input type="password" name="password" required minlength="6">
      </div>

      <div class="form-group">
        <label>Full Name</label>
        <input type="text" name="name" value="<%= userInput?.name || '' %>" required>
      </div>

      <div class="form-group">
        <label>Sex</label>
        <select name="sex" required>
          <option value="">Select</option>
          <option value="Male" <%= userInput?.sex === 'Male' ? 'selected' : '' %>>Male</option>
          <option value="Female" <%= userInput?.sex === 'Female' ? 'selected' : '' %>>Female</option>
        </select>
      </div>

      <div class="form-group">
        <label>Date of Birth</label>
        <input type="date" name="dob" value="<%= userInput?.dob || '' %>" required>
      </div>

      <div class="form-group">
        <label>Address</label>
        <input type="text" name="address" value="<%= userInput?.address || '' %>" required>
      </div>

      <div class="form-group">
        <label>Role</label>
        <select name="role" required>
          <option value="">Select Role</option>
          <option value="student" <%= userInput?.role === 'student' ? 'selected' : '' %>>Student</option>
          <option value="teacher" <%= userInput?.role === 'teacher' ? 'selected' : '' %>>Teacher</option>
        </select>
      </div>

      <button type="submit">Create User</button>
    </form>

    <div class="back-link">
      <a href="/admin/users">Back to Users</a> | 
      <a href="/dashboard">Dashboard</a>
    </div>
  </div>

  <script>
    function previewPhoto(event) {
      const file = event.target.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = e => document.getElementById('preview').src = e.target.result;
        reader.readAsDataURL(file);
      }
    }
  </script>
</body>
</html>