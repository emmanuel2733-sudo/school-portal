<!DOCTYPE html>
<html>
<head>
  <title>Generate Questions From Image</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
  <h3>🤖 Generate Questions From Image</h3>

  <form action="/teacher/question-bank/<%= course_id %>/<%= class_id %>/ai-image"
        method="POST"
        enctype="multipart/form-data"
        class="card p-4 mt-4">

    <div class="mb-3">
      <label class="form-label">Upload Image</label>
      <input type="file" name="image" class="form-control" accept="image/*" required>
    </div>

    <div class="mb-3">
      <label class="form-label">Number of Questions</label>
      <input type="number" name="count" class="form-control" value="5" min="1" max="20">
    </div>

    <button class="btn btn-primary">
      Generate Questions
    </button>
  </form>

  <a href="/teacher/question-bank/<%= course_id %>/<%= class_id %>/questions"
     class="btn btn-link mt-3">
    ← Back to Questions
  </a>
</div>

</body>
</html>
