<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Teacher Profile - <%= teacher.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #f8f9fa; }
    .profile-card { max-width: 600px; margin: 30px auto; padding: 20px; }
    .profile-img { width: 120px; height: 120px; object-fit: cover; border: 4px solid #fff; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
    .info-label { font-weight: 600; color: #495057; }
    .back-btn { background: #6c757d; }
    .back-btn:hover { background: #5a6268; }
  </style>
</head>
<body>

  <div class="container">
    <a href="/admin/search-users" class="btn back-btn text-white mb-3">
      Back to Search
    </a>

    <div class="card profile-card">
      <div class="card-body text-center">
        <img 
          src="<%= teacher.photo ? '/uploads/' + teacher.photo : '/uploads/default-photo.jpg' %>" 
          alt="Teacher Photo" 
          class="rounded-circle profile-img mb-3"
        >
        <h3 class="card-title"><%= teacher.name %></h3>
        <p class="text-muted">@<%= teacher.username %> • <%= teacher.role.charAt(0).toUpperCase() + teacher.role.slice(1) %></p>
        <span class="badge <%= teacher.status === 'approved' ? 'bg-success' : teacher.status === 'pending' ? 'bg-warning' : 'bg-danger' %>">
          <%= teacher.status.charAt(0).toUpperCase() + teacher.status.slice(1) %>
        </span>
      </div>

      <div class="card-body">
        <div class="row g-3">
          <div class="col-md-6">
            <span class="info-label">Sex:</span>
            <p><%= teacher.sex %></p>
          </div>
          <div class="col-md-6">
            <span class="info-label">Date of Birth:</span>
            <p><%= teacher.dob_formatted %> (Age: <%= teacher.age %>)</p>
          </div>
          <div class="col-12">
            <span class="info-label">Address:</span>
            <p class="mb-0"><%= teacher.address || '—' %></p>
          </div>
        </div>
      </div>

      <div class="card-footer text-center">
        <a href="/admin/users/manage/<%= teacher.id %>" class="btn btn-primary">Edit User</a>
        <a href="/teacher/courses?teacherId=<%= teacher.id %>" class="btn btn-info text-white">View Assigned Courses</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>