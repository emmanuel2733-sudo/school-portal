<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>View Files</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- BOOTSTRAP -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- FONT AWESOME 6 — THIS IS THE MISSING LINE! -->
  <link rel="stylesheet" 
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
        integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" 
        crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    body { background: #f8fafc; font-family: 'Segoe UI', sans-serif; }
   .navbar { 
      position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
      background: white !important; padding: 1.2rem 0;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08); 
      border-bottom: 1px solid #e2e8f0;
    }
    .navbar-brand { 
      color: #1a202c !important; font-weight: 800; font-size: 1.8rem; 
     
    
    }
    .back-btn { width: 50px; height: 50px; border-radius: 50%; background: #6366f1; color: white;
      display: flex; align-items: center; justify-content: center; font-size: 1.4rem;
      text-decoration: none; box-shadow: 0 5px 15px rgba(0,0,0,0.15); transition: all 0.3s;
    }
    .back-btn:hover { background: #4f46e5; transform: scale(1.1); }
    .content { margin-top: 100px; padding: 40px 20px; }
    .file-list { background: white; padding: 40px; border: 1px solid #e2e8f0; box-shadow: 0 10px 30px rgba(0,0,0,0.08); }
    .file-item { padding: 15px 0; border-bottom: 1px dashed #e2e8f0; display: flex; justify-content: space-between; align-items: center; }
    .file-item:last-child { border-bottom: none; }
  </style>
</head>
<body>

  <nav class="navbar">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="/dashboard">School Portal</a>
      <div class="d-flex align-items-center gap-3">
        <a href="/teacher/e-library" class="back-btn" title="Back">
          <i class="fas fa-arrow-left"></i>
        </a>
      </div>
    </div>
  </nav>

  <div class="content container">
    <h2 style="font-size:2.4rem;font-weight:800;color:#1a202c;text-align:center;margin-bottom:40px;">
      Files — <%= course_name %> (<%= class_name %>)
    </h2>

    <div class="file-list">
      <% if (files && files.length > 0) { %>
        <% files.forEach(file => { %>
          <div class="file-item">
            <div>
              <i class="fas fa-file-pdf text-danger me-2"></i>
              <strong><%= file.original_name %></strong>
            </div>
           <div class="d-flex gap-3 align-items-center">
  <a href="/uploads/e-library/<%= file.filename %>" 
     target="_blank" 
     class="text-primary">
    <i class="fas fa-download"></i> Download
  </a>

  <form action="/teacher/e-library/delete/<%= file.id %>" 
        method="POST"
        onsubmit="return confirm('Are you sure you want to delete this file?');">
    <button type="submit" class="btn btn-sm btn-danger">
      <i class="fas fa-trash"></i>
    </button>
  </form>
</div>

          </div>
        <% }) %>
      <% } else { %>
        <p class="text-center text-muted fs-3 py-5">No files uploaded yet.</p>
      <% } %>
    </div>
  </div>
</body>
</html>