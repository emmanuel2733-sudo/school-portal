<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    :root {
      --primary: #6366f1;
      --success: #10b981;
      --danger: #e74a3b;
    }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      background: #f8fafc;
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
    }

    /* NAVBAR - SAME AS DASHBOARD */
    .navbar {
      background: white !important;
      padding: 1.2rem 0;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
      border-bottom: 1px solid #e2e8f0;
    }
    .navbar-brand {
      color: #1a202c !important;
      font-weight: 800;
      font-size: 1.8rem;
    }

    /* BUTTONS */
    .power-btn, .back-nav-btn {
      width: 50px; height: 50px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.4rem;
      transition: all 0.3s;
      box-shadow: 0 5px 15px rgba(0,0,0,0.15);
    }
    .power-btn { background: var(--danger); color: white; }
    .power-btn:hover { background: #c82333; transform: scale(1.1); }
    .back-nav-btn { background: var(--primary); color: white; text-decoration: none; }
    .back-nav-btn:hover { background: #4f46e5; transform: scale(1.1); }

    .class-teacher-badge {
      background: linear-gradient(135deg, #ff6b9d, #ff8ab3);
      color: white;
      padding: 10px 24px;
      border-radius: 50px;
      font-weight: 700;
      font-size: 1rem;
      box-shadow: 0 5px 15px rgba(255,107,157,0.4);
    }

    /* PROFILE CARD - PURE WHITE HEADER */
    .profile-container {
      max-width: 700px;
      margin: 50px auto;
      padding: 0 20px;
    }
    .profile-card {
      background: white;
      border: 1px solid #e2e8f0;
      border-radius: 0;
      box-shadow: 0 15px 40px rgba(0,0,0,0.1);
      overflow: hidden;
    }

    /* WHITE HEADER - NO BLUE! */
    .profile-header {
      background: white;
      padding: 50px 30px 40px;
      text-align: center;
      border-bottom: 4px solid #e2e8f0;
    }
    .profile-photo {
      width: 180px;
      height: 180px;
      object-fit: cover;
      border: 10px solid white;
      box-shadow: 0 15px 35px rgba(0,0,0,0.2);
      margin-bottom: 25px;
    }
    .profile-name {
      font-size: 2.5rem;
      font-weight: 800;
      color: #1a202c;
      margin: 0 0 12px 0;
    }
    .profile-username {
      font-size: 1.3rem;
      color: #718096;
      margin-bottom: 18px;
    }
    .status-badge {
      background: var(--success);
      color: white;
      padding: 12px 40px;
      border-radius: 50px;
      font-weight: 700;
      font-size: 1.2rem;
      display: inline-block;
      box-shadow: 0 8px 25px rgba(16,185,129,0.4);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .profile-body {
      padding: 50px;
      background: white;
    }
    .info-row {
      padding: 20px 0;
      border-bottom: 1px dashed #e2e8f0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 1.2rem;
    }
    .info-row:last-child { border-bottom: none; }
    .info-label {
      font-weight: 700;
      color: #4a5568;
      min-width: 160px;
    }
    .info-value {
      font-weight: 600;
      color: #1a202c;
      text-align: right;
      flex: 1;
    }
  </style>
</head>
<body>

  <!-- SAME NAVBAR -->
  <nav class="navbar">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="/dashboard">School Portal</a>
      <div class="d-flex align-items-center gap-3">
        <% if (typeof classTeacherOf !== 'undefined' && classTeacherOf && classTeacherOf.trim() !== '') { %>
          <div class="class-teacher-badge">Class Teacher — <%= classTeacherOf %></div>
        <% } %>
        <a href="/dashboard" class="back-nav-btn" title="Back to Dashboard">
          <i class="fas fa-arrow-left"></i>
        </a>
        <a href="/logout" class="power-btn">
          <i class="fas fa-power-off"></i>
        </a>
      </div>
    </div>
  </nav>

  <div class="profile-container">
    <div class="profile-card">

      <!-- PURE WHITE HEADER -->
      <div class="profile-header">
        <img
          src="<%= teacher.photo ? '/uploads/' + teacher.photo : '/uploads/default-photo.jpg' %>"
          alt="Profile Photo"
          class="profile-photo rounded-circle"
          onerror="this.src='/uploads/default-photo.jpg'"
        >
        <h1 class="profile-name"><%= teacher.name || teacher.username %></h1>
        <p class="profile-username">@<%= teacher.username %> • Teacher</p>
        <div class="status-badge">Approved</div>
      </div>



      <!-- PERSONAL INFORMATION -->
      <div class="profile-body">
        <div class="info-row">
          <span class="info-label">Full Name</span>
          <span class="info-value"><%= teacher.name || 'Not set' %></span>
        </div>
        <div class="info info-row">
          <span class="info-label">Gender</span>
          <span class="info-value"><%= teacher.sex || 'Not set' %></span>
        </div>
        <div class="info-row">
          <span class="info-label">Date of Birth</span>
          <span class="info-value">
            <%= teacher.dob_formatted || '—' %>
            <% if (teacher.age) { %> (Age: <%= teacher.age %>)<% } %>
          </span>
        </div>
        <div class="info-row">
          <span class="info-label">Address</span>
          <span class="info-value"><%= teacher.address || 'Not provided' %></span>
        </div>
      </div>

    </div>
  </div>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>