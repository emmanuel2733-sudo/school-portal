<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Re-enroll Students for Current Term</title>

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Optional: Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to bottom right, #f8f9fa, #e9ecef);
      min-height: 100vh;
      padding: 2rem 0;
    }
    .container {
      max-width: 800px;
    }
    .card {
      border: none;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }
    .card-header {
      background: linear-gradient(135deg, #0d6efd, #0b5ed7);
      color: white;
      padding: 1.5rem;
      border: none;
    }
    .card-header h2 {
      margin: 0;
      font-weight: 600;
    }
    .btn-primary {
      background-color: #0d6efd;
      border: none;
      padding: 0.75rem 2rem;
      font-size: 1.1rem;
      border-radius: 50px;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background-color: #0b5ed7;
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(13, 110, 253, 0.3);
    }
    .btn-secondary {
      border-radius: 50px;
      padding: 0.75rem 2rem;
    }
    .text-warning {
      background-color: #fff3cd;
      padding: 1rem;
      border-radius: 10px;
      border-left: 5px solid #ffc107;
    }
    .tip-box {
      background-color: #e3f2fd;
      padding: 1rem;
      border-radius: 10px;
      border-left: 5px solid #2196f3;
      margin-top: 2rem;
    }
  </style>
</head>
<body>

<div class="container mt-5">
  <div class="card">
    <div class="card-header text-center">
      <h2 class="mb-0"><i class="bi bi-people-fill me-2"></i>Re-enroll All Students for Current Term</h2>
    </div>
    
    <div class="card-body p-5">

      <% if (successMsg) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <i class="bi bi-check-circle-fill"></i> <%= successMsg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>
      <% if (errorMsg) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <i class="bi bi-exclamation-triangle-fill"></i> <%= errorMsg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% } %>

      <!-- <div class="text-center mb-4">
        <h4>Current Term: <span class="badge bg-primary fs-5"><%= termName %></span></h4>
      </div> -->

      <p class="lead text-center mb-4">
        This action will <strong>re-enroll every student</strong> into <strong>all courses</strong> they were previously enrolled in.
      </p>

      <div class="text-warning mb-4">
        <strong>Ideal for:</strong> Starting a new semester/year with the same class structure as before.
      </div>

      <form method="POST" action="/admin/re-enroll-all" 
            onsubmit="return confirm('⚠️ WARNING ⚠️\n\nThis will re-enroll ALL students into their previous courses for <%= termName %>.\n\nAre you absolutely sure?');">
        <div class="d-grid d-md-flex justify-content-center gap-3">
          <button type="submit" class="btn btn-outline-secondary btn-lg">
            <i class="bi bi-arrow-repeat me-2"></i>Re-enroll All Students
          </button>
          <a href="/dashboard" class="btn btn-outline-secondary btn-lg">
            Back to Dashboard
          </a>
        </div>
      </form>

      <div class="tip-box mt-5">
        <small class="text-muted">
          <i class="bi bi-lightbulb-fill text-primary"></i>
          <strong>Tip:</strong> You can also enroll students individually using the 
          "Enroll Students" button inside each class on the dashboard.
        </small>
      </div>
    </div>
  </div>
</div>

<!-- Bootstrap 5 JS (for alerts to be dismissible) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>