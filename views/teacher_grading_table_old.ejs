<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Grade: <%= course.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root { --primary: #4e73df; --success: #28a745; --warning: #ffc107; --danger: #dc3545; }
    body { background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%); font-family: 'Segoe UI', sans-serif; }
    .card { border-radius: 18px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
    .table th { background: var(--primary); color: white; }
    .score-input { width: 70px; text-align: center; }
    .total { font-weight: 700; color: var(--primary); }
    .gpa { background: var(--success); color: white; font-weight: 700; }
    .btn-save { background: var(--success); color: white; border-radius: 50px; padding: 10px 30px; }
  </style>
</head>
<body>
  <div class="container mt-4">
    <div class="card p-4">
      <h3 class="text-center mb-2"><%= course.name %> - <%= course.class_name %></h3>
      <p class="text-center text-muted">CA = 30 | Exam = 70 | Total = 100</p>

      <form method="POST" action="/teacher/grade/course/<%= course.id %>/save">
        <table class="table table-bordered table-hover">
          <thead>
            <tr>
              <th>S/N</th>
              <th>Student Name</th>
              <th>CA (30)</th>
              <th>Exam (70)</th>
              <th>Total</th>
              <th>Grade</th>
            </tr>
          </thead>
          <tbody>
            <% students.forEach((s, i) => { 
              const g = gradeMap[s.student_id] || { ca: '', exam: '' };
              const total = (parseInt(g.ca) || 0) + (parseInt(g.exam) || 0);
              let grade = '';
              if (total >= 70) grade = 'A';
              else if (total >= 60) grade = 'B';
              else if (total >= 50) grade = 'C';
              else if (total >= 40) grade = 'D';
              else grade = 'F';
            %>
              <tr>
                <td><%= i + 1 %></td>
                <td><strong><%= s.name %></strong></td>
                <td><input type="number" name="ca_<%= s.student_id %>" class="form-control score-input" value="<%= g.ca %>" min="0" max="30"></td>
                <td><input type="number" name="exam_<%= s.student_id %>" class="form-control score-input" value="<%= g.exam %>" min="0" max="70"></td>
                <td class="total"><%= total %></td>
                <td class="gpa"><%= grade %></td>
                <input type="hidden" name="student_<%= s.student_id %>" value="1">
              </tr>
            <% }) %>
          </tbody>
        </table>

        <div class="text-center">
          <button type="submit" class="btn btn-save">Save All Grades</button>
          <a href="/teacher/courses" class="btn btn-secondary ms-3">Back fake</a>
        </div>
      </form>
    </div>
  </div>

  <script>
    // Auto-calculate total & grade
    document.querySelectorAll('input[type=number]').forEach(input => {
      input.addEventListener('input', () => {
        const row = input.closest('tr');
        const ca = parseInt(row.querySelector('input[name^="ca_"]').value) || 0;
        const exam = parseInt(row.querySelector('input[name^="exam_"]').value) || 0;
        const total = ca + exam;
        let grade = total >= 70 ? 'A' : total >= 60 ? 'B' : total >= 50 ? 'C' : total >= 40 ? 'D' : 'F';
        row.querySelector('.total').textContent = total;
        row.querySelector('.gpa').textContent = grade;
      });
    });
  </script>
</body>
</html> -->