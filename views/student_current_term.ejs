<!DOCTYPE html>
<html lang="en" data-theme="light">
<%- include('partials/student_head', { title: 'My Results' }) %>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Results</title>
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --p:#ff6b9d; --pink-dark:#e55a87; --c:#fdf5e6; --l:#ffe6f0; --g:#6c757d; --r:14px;
      --text:#222; --table-header:#ffe6f0; --table-border:#ffd4e4; --summary-bg:#ffe6f0;
      --pending:#e67e22; --muted:#999;
    }
    [data-theme="dark"] {
      --c:#1a1a1a; --l:#2c3e50; --text:#ecf0f1; --table-header:#2d1b3a; --table-border:#444; --summary-bg:#2d1b3a;
      background:#111!important; color:#ecf0f1!important;
    }

    body { font-family:'Poppins',sans-serif; background:var(--c); padding:20px; min-height:100vh; margin:0; color:var(--text); }
    .container { max-width:1100px; margin:0 auto; }

    .top-bar {
      display: flex; justify-content: space-between; align-items: center;
      padding: 18px 0; margin-bottom: 20px; border-bottom: 1px solid #ddd;
    }
    [data-theme="dark"] .top-bar { border-color: #444; }

    .session-info { font-size: 1.1rem; font-weight: 600; opacity: 0.95; }
    .top-controls { display: flex; align-items: center; gap: 12px; }

    .theme-toggle {
      background: #6c757d; color: #fff; width: 40px; height: 40px; border-radius: 50%;
      border: none; font-size: 1.2rem; cursor: pointer; display: flex; align-items: center; justify-content: center;
    }
    .back-btn, .logout-btn {
      padding: 10px 18px; border-radius: 8px; text-decoration: none; font-weight: 600; font-size: 0.95rem;
    }
    .back-btn { background: var(--p); color: #fff; }
    .back-btn:hover { background: var(--pink-dark); }
    .logout-btn { background: #dc3545; color: #fff; }
    .logout-btn:hover { background: #c82333; }

    /* ANNUAL CGPA BAR - Directly under session info */
    .cgpa-inline-bar {
      text-align: left;
      padding: 14px 20px;
      background: rgba(255, 107, 157, 0.12);
      border: 1px solid rgba(255, 107, 157, 0.25);
      border-radius: 12px;
      margin: 0 0 30px 0;
      font-size: 1rem;
      font-weight: 500;
      display: block;
      width: 100%;
      box-sizing: border-box;
    }
    [data-theme="dark"] .cgpa-inline-bar {
      background: rgba(255, 107, 157, 0.18);
      border-color: rgba(255, 107, 157, 0.4);
    }

    .highlight {
      color: #ff6b9d;
      font-weight: 800;
      font-size: 1.2rem;
    }
    .separator {
      margin: 0 14px;
      color: #aaa;
      font-weight: bold;
    }

    /* Rest of your styles (term cards, tables, etc.) */
    .term-card {
      border: 2px solid var(--l); border-radius: var(--r); overflow: hidden; margin-bottom: 28px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08); background: white;
    }
    [data-theme="dark"] .term-card { background: #222; border-color: #444; }

    .term-header {
      background: var(--p); color: white; padding: 18px 22px; cursor: pointer;
      font-size: 1.3rem; font-weight: 700; display: flex; justify-content: space-between; align-items: center;
    }
    .term-header:hover { background: #ff5d8f; }
    .term-header.active { background: var(--pink-dark); }

    .term-name { font-size: 1.35rem; }
    .term-badge {
      background: rgba(255,255,255,0.35); padding: 6px 12px; border-radius: 20px;
      font-size: 0.8rem; font-weight: 600; margin-left: 12px;
    }
    .pending-badge { background: #e67e22; color: white; }
    .complete-badge { background: #50c878; color: white; }

    .term-info {
      font-size: 1.05rem; font-weight: 600; text-shadow: 0 1px 2px rgba(0,0,0,0.3);
    }

    .term-content {
      max-height: 0; overflow: hidden; transition: max-height 0.6s ease, padding 0.6s ease;
      background: white;
    }
    [data-theme="dark"] .term-content { background: #1e1e1e; }
    .term-content.open { max-height: 6000px; padding: 26px 22px; }

    table { width: 100%; border-collapse: collapse; margin: 15px 0; font-size: 0.95rem; }
    th { background: var(--table-header); color: #333; padding: 14px 12px; text-align: left; font-weight: 600; }
    [data-theme="dark"] th { color: #e0e0e0; }
    td { padding: 12px; border-bottom: 1px solid var(--table-border); color: var(--text); }
    tr:hover { background: #fff9fb; }
    [data-theme="dark"] tr:hover { background: #2a2a2a; }

    .pending-text { color: var(--pending); font-style: italic; }
    .muted { color: var(--muted); }

    .grade-A { color: #50c878 !important; font-weight: bold; }
    .grade-B { color: #3498db !important; font-weight: bold; }
    .grade-C { color: #f1c40f !important; font-weight: bold; }
    .grade-D { color: #e67e22 !important; font-weight: bold; }
    .grade-F { color: #e74c3c !important; font-weight: bold; }

    .summary-card {
      text-align: center; padding: 18px; background: var(--summary-bg);
      border-radius: var(--r); font-size: 1.25rem; font-weight: 700; color: var(--p);
      margin: 20px 0; border: 1px solid var(--l);
    }
    /* ===== NAV HEADER (LYTEBRIDGE STYLE) ===== */
.top-bar {
  position: fixed;
  top: 0; left: 0; right: 0;
  background: var(--card-bg, #ffffff);
  padding: 1.2rem 1.5rem;
  box-shadow: 0 6px 16px rgba(255, 107, 157, 0.15);
  border-bottom: 1px solid #ffe0e9;
  z-index: 1030;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.school-name {
  font-size: 1.8rem;
  font-weight: 800;
  background: linear-gradient(90deg, #ff6b9d, #ff8ab3);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.top-controls {
  display: flex;
  align-items: center;
  gap: 16px;
}

/* Dark mode toggle (slider) */
.theme-toggle {
  width: 52px;
  height: 28px;
  border-radius: 50px;
  border: 2px solid #ffe0e9;
  background: var(--card-bg, #ffffff);
  position: relative;
  cursor: pointer;
}

.theme-toggle::before {
  content: '';
  position: absolute;
  top: 4px;
  left: 4px;
  width: 20px;
  height: 20px;
  background: #ff6b9d;
  border-radius: 50%;
  transition: transform 0.3s ease;
}

[data-theme="dark"] .theme-toggle::before {
  transform: translateX(24px);
}

/* Buttons */
.back-btn,
.logout-btn {
  padding: 10px 18px;
  border-radius: 50px;
  font-weight: 600;
  text-decoration: none;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: 0.3s ease;
}

.back-btn {
  border: 2px solid #ff6b9d;
  color: var(--text, #333);
}

.back-btn:hover {
  background: #ff6b9d;
  color: #fff;
}

.logout-btn {
  background: #ff6b9d;
  color: #fff;
  box-shadow: 0 6px 16px rgba(255, 107, 157, 0.3);
}

.logout-btn:hover {
  background: #e55a87;
}
.main-content,
.container {
  margin-top: 100px;
}


  </style>
</head>
<body>
  <!-- TOP NAV BAR -->
<div class="top-bar">
  <div class="school-name">LYTEBRIDGE ACADEMY</div>

  <div class="top-controls">
    <!-- Dark Mode Toggle -->
    <button class="theme-toggle" id="theme-toggle" aria-label="Toggle dark mode"></button>

    <!-- Back Button -->
    <a href="/dashboard" class="back-btn">
      <i class="fas fa-arrow-left"></i> Back
    </a>

    <!-- Logout (optional) -->
    <a href="/logout" class="logout-btn">
      <i class="fas fa-sign-out-alt"></i> Logout
    </a>
  </div>
</div>

  <div class="container">

    <div class="top-bar">
      <div class="session-info">
        Academic Session: <strong><%= locals.currentYear || '2025/2026' %></strong> • 
        Class: <strong><%= locals.studentClass || 'Not Assigned' %></strong>
      </div>
      <div class="top-controls">
        <button class="theme-toggle" id="theme-toggle">Moon</button>
        <a href="/dashboard" class="back-btn">Back to Dashboard </a>
        <!-- <a href="/logout" class="logout-btn">Logout</a> -->
      </div>
    </div>

    <!-- ANNUAL CGPA BAR - PERFECTLY PLACED UNDER SESSION INFO -->
    <div class="cgpa-inline-bar">
      <span>
        <strong>Annual CGPA:</strong> 
        <span class="highlight"><%= locals.yearlyCGPA !== null ? locals.yearlyCGPA : '-' %></span>
      </span>
      <span class="separator">•</span>
      <span>
        <strong>Overall Position:</strong> 
        <span class="highlight"><%= locals.yearlyPosition !== null ? locals.yearlyPosition : '-' %></span>
      </span>
    </div>

    <% const termsData = locals.termsData || []; %>

    <% if (termsData.length === 0) { %>
      <div style="text-align:center; padding:50px; color:#999;">
        <h3>No Active Academic Session</h3>
        <p>Please contact your school administrator.</p>
      </div>
    <% } else { %>

      <% 
        const termOrder = { 'first term':1, '1st term':1, 'first':1, 'second term':2, '2nd term':2, 'second':2, 'third term':3, '3rd term':3, 'third':3 };
        const orderedTerms = [...termsData].sort((a, b) => {
          const aKey = a.name?.toLowerCase().trim() || '';
          const bKey = b.name?.toLowerCase().trim() || '';
          return (termOrder[aKey] || 99) - (termOrder[bKey] || 99);
        });
      %>

      <% orderedTerms.forEach(term => { %>
        <div class="term-card">
          <div class="term-header <%= term.is_current ? 'active' : '' %>"
               onclick="this.classList.toggle('active'); this.nextElementSibling.classList.toggle('open')">
            <div>
              <span class="term-name"><%= term.name %></span>
              <% if (term.is_current) { %>
                <span class="term-badge">CURRENT TERM</span>
              <% } %>
              <% if (term.scoresComplete) { %>
                <span class="term-badge complete-badge">COMPLETED</span>
              <% } else { %>
                <span class="term-badge pending-badge">PENDING</span>
              <% } %>
            </div>

            <div class="term-info">
              <% if (term.scoresComplete && term.gpa !== null) { %>
                GPA: <strong><%= term.gpa %></strong> • Position: <strong><%= term.position %></strong>
              <% } else { %>
                <em style="opacity:0.9;">Awaiting teacher scores...</em>
              <% } %>
            </div>
          </div>

          <div class="term-content <%= term.is_current ? 'open' : '' %>">
            <% if (term.courses && term.courses.length > 0) { %>
              <table>
                <thead>
                  <tr>
                    <th>S/N</th>
                    <th>Subject</th>
                    <th>CA (30)</th>
                    <th>Exam (70)</th>
                    <th>Total</th>
                    <th>Grade</th>
                  </tr>
                </thead>
                <tbody>
                  <% term.courses.forEach((c, i) => { %>
                    <% const r = term.results[c.course_id] || {}; %>
                    <% const hasScore = r.ca !== null && r.exam !== null; %>
                    <tr>
                      <td><%= i + 1 %></td>
                      <td><strong><%= c.course_name %></strong></td>
                      <td><%= hasScore ? r.ca : '<span class="pending-text">Pending</span>' %></td>
                      <td><%= hasScore ? r.exam : '<span class="pending-text">Pending</span>' %></td>
                      <td><% if (hasScore && r.total) { %><strong><%= r.total %></strong><% } else { %><span class="pending-text">—</span><% } %></td>
                      <td><% if (hasScore && r.grade) { %><span class="grade-<%= r.grade %>"><%= r.grade %></span><% } else { %><span class="muted">—</span><% } %></td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>

              <div class="summary-card">
                <% if (term.scoresComplete && term.gpa !== null) { %>
                  <%= term.name %> Results • GPA: <strong><%= term.gpa %></strong> • Position: <strong><%= term.position %></strong>
                <% } else { %>
                  <em style="color:var(--pending)">Results incomplete — waiting for all subject scores</em>
                <% } %>
              </div>
            <% } else { %>
              <p style="text-align:center; color:#999; padding:20px;">No courses assigned for this term yet.</p>
            <% } %>
          </div>
        </div>
      <% }) %>

    <% } %>
  </div>
<script>
  const toggle = document.getElementById('theme-toggle');
  const html = document.documentElement;

  const savedTheme = localStorage.getItem('theme') || 'light';
  html.setAttribute('data-theme', savedTheme);

  toggle.addEventListener('click', () => {
    const next = html.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
    html.setAttribute('data-theme', next);
    localStorage.setItem('theme', next);
  });
</script>



  <script>
    const toggle = document.getElementById('theme-toggle');
    const saved = localStorage.getItem('theme') || 'light';
    document.documentElement.setAttribute('data-theme', saved);
    toggle.innerHTML = saved === 'dark' ? 'Sun' : 'Moon';

    toggle.onclick = () => {
      const current = document.documentElement.getAttribute('data-theme');
      const next = current === 'dark' ? 'light' : 'dark';
      document.documentElement.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);
      toggle.innerHTML = next === 'dark' ? 'Sun' : 'Moon';
    };

    // Auto-expand current term
    document.querySelectorAll('.term-header.active').forEach(h => {
      h.nextElementSibling.classList.add('open');
    });
  </script>
</body>
</html>