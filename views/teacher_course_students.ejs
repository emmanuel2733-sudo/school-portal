<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= course.name %> - Students</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background: #f8f9fa; font-family: 'Segoe UI', sans-serif; }
    .card { border-radius: 15px; box-shadow: 0 5px 20px rgba(0,0,0,0.1); }
    .table th { background: #4e73df; color: white; }
    .btn-grade { font-size: 0.9rem; padding: 5px 12px; }
  </style>
</head>
<body>

  <div class="container mt-4">
    <div class="card p-4">
      <h3><%= course.name %> - <%= course.class %></h3>
      <p class="text-muted">Manage student grades below</p>

      <% if (students.length === 0) { %>
        <p class="text-center text-muted">No students enrolled.</p>
      <% } else { %>
        <table class="table table-hover">
          <thead>
            <tr>
              <th>Student</th>
              <th>Action</th>
            </tr>
          </thead>
          <tbody>
            <% students.forEach(s => { %>
              <tr>
                <td><%= s.student_name %></td>
                <td>
                  <a href="/teacher/grade/<%= course.id %>/student/<%= s.student_id %>" class="btn btn-primary btn-grade">
                    Manage Grade
                  </a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } %>

      <div class="mt-4">
        <a href="/teacher/courses" class="btn btn-secondary">Back to Courses</a>
      </div>
    </div>
  </div>

</body>
</html>