<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Upload File</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Font Awesome 6 (MUST be included!) -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" 
        integrity="sha512-..." crossorigin="anonymous" referrerpolicy="no-referrer" />

  <style>
    body { 
      background: #f8fafc; 
      font-family: 'Segoe UI', sans-serif; 
    }
    .navbar { 
      position: fixed; top: 0; left: 0; right: 0; z-index: 1000;
      background: white !important; padding: 1.2rem 0;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08); 
      border-bottom: 1px solid #e2e8f0;
    }
    .navbar-brand { 
      color: #1a202c !important; font-weight: 800; font-size: 1.8rem; 
    }

    /* FIXED BACK BUTTON - Now perfectly visible */
    .back-btn { 
      width: 50px; 
      height: 50px; 
      border-radius: 50%; 
      background: #6366f1; 
      color: white !important;           /* Bright white icon */
      display: flex; 
      align-items: center; 
      justify-content: center; 
      font-size: 1.4rem;
      text-decoration: none; 
      box-shadow: 0 5px 15px rgba(0,0,0,0.15); 
      transition: all 0.3s;
    }
    .back-btn:hover { 
      background: #4f46e5; 
      transform: scale(1.1); 
      color: white !important;
    }

    .content { margin-top: 100px; padding: 40px 20px; }
    .upload-card { background: white; padding: 50px; border: 1px solid #e2e8f0; box-shadow: 0 10px 30px rgba(0,0,0,0.08); text-align: center; }
    .upload-btn { background: #10b981; color: white; padding: 15px 50px; font-size: 1.3rem; font-weight: 700; border: none; border-radius: 8px; }
    .upload-btn:hover { background: #059669; }
  </style>
</head>
<body>

  <nav class="navbar">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="/dashboard">School Portal</a>
      <div class="d-flex align-items-center gap-3">
        <a href="/teacher/e-library" class="back-btn" title="Back">
          <i class="fas fa-arrow-left"></i>
        </a>
      </div>
    </div>
  </nav>

  <div class="content container">
    <div class="upload-card">
      <h3 style="font-size:2rem;font-weight:800;color:#1a202c;margin-bottom:30px;">
        Upload File — <%= course_name %> (<%= class_name %>)
      </h3>
      <form action="/teacher/e-library/upload" method="POST" enctype="multipart/form-data">
        <input type="hidden" name="course_id" value="<%= course_id %>">
        <div class="mb-4">
          <input type="file" name="document" class="form-control form-control-lg" accept=".pdf,.doc,.docx" required>
        </div>
        <button type="submit" class="upload-btn">
          <i class="fas fa-upload"></i> Upload Document
        </button>
      </form>
    </div>
  </div>
</body>
</html>