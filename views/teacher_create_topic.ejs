<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Create Topic</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    body { background: #f7f7f8; font-family: 'Segoe UI', sans-serif; }
    .navbar-brand { color: #1a202c !important; font-weight: 800; font-size: 1.8rem; }
    .power-btn, .back-nav-btn {
      width: 50px; height: 50px; border-radius: 50%; display: flex;
      align-items: center; justify-content: center; font-size: 1.4rem;
      transition: all 0.3s; box-shadow: 0 5px 15px rgba(0,0,0,0.15);
    }
    .power-btn { background: #e74a3b; color: white; }
    .power-btn:hover { background: #c82333; transform: scale(1.1); }
    .back-nav-btn { background: #6366f1; color: white; text-decoration: none; }
    .back-nav-btn:hover { background: #4f46e5; transform: scale(1.1); }
    .class-teacher-badge {
      background: linear-gradient(135deg, #ff6b9d, #ff8ab3); color: white;
      padding: 10px 24px; border-radius: 50px; font-weight: 700; font-size: 1rem;
      box-shadow: 0 5px 15px rgba(255,107,157,0.4);
    }
    .content { margin-top: 100px; padding: 40px 20px; }
    .card-block { background: white; padding: 40px; border: 1px solid #e2e8f0; box-shadow: 0 10px 30px rgba(0,0,0,0.08); }
    .topic-input { margin-bottom: 20px; }
    .save-btn { background: #10b981; color: white; padding: 15px 40px; border: none; font-weight: 700; font-size: 1.2rem; }
    .save-btn:hover { background: #059669; }
  </style>
</head>
<body>

  <nav class="navbar">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="/dashboard">School Portal</a>
      <div class="d-flex align-items-center gap-3">
        <a href="/teacher/topics" class="back-nav-btn"><i class="fas fa-arrow-left"></i></a>
        <!-- <a href="/logout" class="power-btn"><i class="fas fa-power-off"></i></a> -->
      </div>
    </div>
  </nav>

  <div class="content container">
    <div class="card-block">
      <h3 style="font-size:2rem;font-weight:800;color:#1a202c;margin-bottom:30px;">
        Create Topics — <%= course_name %> (<%= class_name %>)
      </h3>

      <form action="/teacher/topics/save" method="POST">
        <input type="hidden" name="course_id" value="<%= course_id %>">

        <% for(let i = 1; i <= 22; i++) { %>
          <div class="topic-input">
            <label style="font-weight:600;color:#4a5568;">Topic <%= i %></label>
            <input type="text" name="topics[]" class="form-control" placeholder="Enter topic name">
          </div>
        <% } %>

        <div class="text-center">
          <button type="submit" class="save-btn">Save All Topics</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>