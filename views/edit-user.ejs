<!DOCTYPE html>
<html>
<head>
  <title>Edit User - <%= user.name %></title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body { font-family: Arial; background: #f4f6f9; padding: 20px; }
    .card { max-width: 550px; margin: 30px auto; background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
    h1 { text-align: center; color: #2c3e50; }
    .current-photo { text-align: center; margin: 20px 0; }
    .current-photo img { width: 150px; height: 150px; object-fit: cover; border-radius: 50%; border: 4px solid #ddd; }
    .msg { padding: 12px; border-radius: 6px; margin-bottom: 20px; text-align: center; }
    .success { background: #d4edda; color: #155724; }
    .error { background: #f8d7da; color: #721c24; }
    label { display: block; margin: 18px 0 8px; font-weight: 600; }
    input, select { width: 100%; padding: 11px; border: 1px solid #ddd; border-radius: 8px; }
    button { margin-top: 25px; padding: 13px; background: #007bff; color: white; border: none; border-radius: 8px; width: 100%; font-size: 16px; cursor: pointer; }
    button:hover { background: #0056b3; }
    .back { display: block; text-align: center; margin-top: 20px; color: #007bff; text-decoration: none; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Edit User</h1>

    <% if (success) { %>
      <div class="msg success"><%= success %></div>
    <% } %>
    <% if (error) { %>
      <div class="msg error"><%= error %></div>
    <% } %>

    <div class="current-photo">
      <p><strong>Current Photo</strong></p>
      <img src="/uploads/<%= user.photo || 'default-photo.jpg' %>" alt="Profile">
    </div>

    <form action="/admin/users/edit/<%= user.id %>" method="POST" enctype="multipart/form-data">
      <input type="hidden" name="currentPhoto" value="<%= user.photo || 'default-photo.jpg' %>">

      <label>Full Name</label>
      <input type="text" name="name" value="<%= user.name %>" required>

      <label>Username</label>
      <input type="text" name="username" value="<%= user.username %>" required>

      <label>Date of Birth</label>
      <input type="date" name="dob" value="<%= user.dob ? user.dob.split('T')[0] : '' %>">

      <label>Address</label>
      <input type="text" name="address" value="<%= user.address || '' %>" required>

      <label>Change Photo (JPG/PNG, max 2MB)</label>
      <input type="file" name="photo" accept="image/jpeg,image/png">

      <button type="submit">Save Changes</button>
    </form>

    <a href="/admin/users" class="back">Back to Manage Users</a>
  </div>
</body>
</html>